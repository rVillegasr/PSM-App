<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="fcfm.psm.psm_app.ForgottenPasswordActivity"
    android:background="#424242">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textEmailAddress"
        android:ems="10"
        android:id="@+id/txt_fgEmail"
        android:layout_below="@+id/tv_instruction"
        android:hint="Correo Electronico"
        android:textColorHint="#656565"
        android:textColor="#000000"
        android:textSize="20dp"
        android:textIsSelectable="true" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Introduzca su correo electronico para recuperar su contraseña."
        android:id="@+id/tv_instruction"
        android:textAlignment="center"
        android:textSize="20dp"
        android:textColor="#f0c23b"
        android:layout_centerInParent="true" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="Enviar"
        android:id="@+id/tv_eventDescription"
        android:layout_marginTop="50dp"
        android:layout_below="@+id/txt_fgEmail"
        android:textSize="20dp"
        android:background="#303030"
        android:textColor="#ffffff" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/tv_instruction"
        android:layout_alignParentTop="true">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/img_logo"
            android:layout_centerHorizontal="true"
            android:src="@mipmap/ic_launcher" />
    </RelativeLayout>

</RelativeLayout>
